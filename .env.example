# API provider keys (keep values in local .env, never commit real keys)
GROQ_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# Dashboard configuration
NEXT_PUBLIC_ORCHESTRATOR_URL=http://localhost:4000

# Orchestrator runtime
TEXLIVE_URL=http://texlive:5001/build
